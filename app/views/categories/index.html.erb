<%= content_for :nav do %>
  <%= render 'nav_content' %>
  <% if user_signed_in? %>
    <%= link_to "Add a Category",
        new_category_path(user_id: current_user.id),
        class: "list-group-item" %>
  <% end %>
<% end %>
