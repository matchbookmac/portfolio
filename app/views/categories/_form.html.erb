<%= render "layouts/errors", object: @category %>

<%= bootstrap_form_for @category do |f| %>
  <%= f.text_field :language %>
  <%= f.text_field :description %>
  <% if params[:user_id] %>
    <%= hidden_field :user, :id, name: "category[user_ids][]" %>
  <% end %>
  <%= f.submit class: "btn btn-success btn-block" %>
<% end %>
